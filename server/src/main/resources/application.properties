spring.application.name=instaKing


# Data source
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/instaKing
spring.datasource.username=postgres
spring.datasource.password=postgres

#Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.open-in-view=true
spring.jpa.hibernate.ddl-auto=update



spring.mvc.hiddenmethod.filter.enabled=true

debug=true
logging.level.org.springframework.web=DEBUG
#logging.level.org.springframework.security=DEBUG

# ????????? Elasticsearch
spring.elasticsearch.uris=http://localhost:9200
spring.elasticsearch.username=elastic_user
spring.elasticsearch.password=ZjuCv5xT-snJImCOwvPM

spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# JWT secrets (Base64-encoded HS512 keys). Do NOT commit real secrets.
# For local dev, set via environment variables or a non-committed properties file.
# security.jwt.access-secret-base64=${JWT_ACCESS_SECRET_BASE64}
# security.jwt.refresh-secret-base64=${JWT_REFRESH_SECRET_BASE64}